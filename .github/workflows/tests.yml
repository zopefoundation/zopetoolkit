# Generated with zope.meta (https://zopemeta.readthedocs.io/) from:
# https://github.com/zopefoundation/meta/tree/master/src/zope/meta/toolkit
name: tests

on:
  push:
  pull_request:
  schedule:
    - cron: '0 12 * * 0'  # run once a week on Sunday
  # Allow to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: read
      pull-requests: write
    strategy:
      # We want to see all failures:
      fail-fast: false
      matrix:
        os:
        - ["ubuntu", "ubuntu-latest"]
        config:
        # [Python version, tox env]
        - ["3.11", "release-check"]
        - ["3.10", "py310"]
        - ["3.11", "py311"]
        - ["3.12", "py312"]
        - ["3.13", "py313"]
        - ["3.14", "py314"]
        - ["3.15", "py315"]
        - ["pypy-3.11", "pypy3"]
        - ["3.11", "docs"]

    runs-on: ${{ matrix.os[1] }}
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name
    name: ${{ matrix.config[1] }}
    steps:
    - uses: actions/checkout@v7
      with:
        persist-credentials: false
    - name: Install uv + caching
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # version: 9.0.0
      with:
        enable-cache: true
        cache-dependency-glob: |
          setup.*
          tox.ini
        python-version: ${{ matrix.config[0] }}
        github-token: ${{ secrets.GITHUB_TOKEN }}
    - name: Test
      run: uvx --with tox-uv tox -e ${{ matrix.config[1] }}
    - name: Coverage
      if: matrix.config[1] == 'coverage'
      run: |
        uvx coveralls --service=github
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Build package artifacts
      if: >
        matrix.os[0] == 'ubuntu'
        && matrix.config[1] == 'py314'
      run: |
        rm -f dist/*
        pip install -U packaging "setuptools >= 78.1.1,< 82" wheel twine
        python setup.py sdist
        python setup.py bdist_wheel

    - name: Upload package wheel
      if: >
        matrix.os[0] == 'ubuntu'
        && matrix.config[1] == 'py314'
      uses: actions/upload-artifact@v7
      with:
        name: zopetoolkit.whl
        path: dist/*whl

    - name: Upload package source distribution
      if: >
        matrix.os[0] == 'ubuntu'
        && matrix.config[1] == 'py314'
      uses: actions/upload-artifact@v7
      with:
        name: zopetoolkit.tar.gz
        path: dist/*gz

